/**
 * MapModal.web.tsx — web stub
 *
 * react-native-maps is a native-only package and cannot run in a browser.
 * Metro resolves this file instead of MapModal.tsx when bundling for web,
 * so the native import never reaches the web bundle.
 */

export default function MapModal() {
  return null;
}

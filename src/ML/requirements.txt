# Core ML stack
torch>=2.1.0
ultralytics>=8.0.0

# Image and video
Pillow>=10.0.0
opencv-python>=4.8.0

# Flash attention (optional - ~2x inference speed on CUDA GPU, compile-heavy install)
# flash-attn>=2.3.0

# Voice confirmation (text-to-speech + speech recognition)
pyttsx3>=2.90          # offline TTS, no API key required
SpeechRecognition>=3.10.0  # STR via Google Web Speech API (free tier)
pyaudio>=0.2.13        # microphone input for SpeechRecognition

# Backend API client
requests>=2.31.0

# System volume / mute detection (Windows Core Audio API wrapper)
pycaw>=0.6.0
